{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "docker",
  "password": "docker",
  "database": "postgres",
  "entities": ["./dist/**/*.entity{.ts,.js}"],
  "migrations": ["./dist/**/*.ts"],
  "synchronize": true,
  "logging": true,
  "cli": {
    "migrationsDir": "./src/shared/infra/typeorm/migrations"
  }
}
