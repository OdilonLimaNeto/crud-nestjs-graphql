{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "",
  "password": "",
  "database": "",
  "entities": ["./dist/**/*.entity{.ts,.js}"],
  "migrations": ["./dist/**/*.ts"],
  "synchronize": true,
  "logging": true,
  "cli": {
    "migrationsDir": "./src/shared/infra/typeorm/migrations"
  }
}
